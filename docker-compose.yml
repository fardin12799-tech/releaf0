version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: releaf-mysql
    environment:
      MYSQL_ROOT_PASSWORD: A1dmin!lie
      MYSQL_DATABASE: releaf_db
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
